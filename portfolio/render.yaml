services:
  - type: web
    name: daek-portfolio
    env: node
    buildCommand: npm install && npm run build
    startCommand: next start -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: RESEND_API_KEY
        sync: false
